from typing import List



# class TokenizedState:
#     def __init__(self, text_token_ids, graph_token_ids):
#         self.text_token_ids: List[int] = text_token_ids
#         self.graph_token_ids: List[int] = graph_token_ids
#
#
# # class ListOfTokenizedStates(Dataset):
# #     def __init__(self, text_token_ids):
# #         self.token_ids: List[List[int]] = text_token_ids
# #
# #     def append(self, state_ids: List[int]):
# #         self.token_ids.append(state_ids)
